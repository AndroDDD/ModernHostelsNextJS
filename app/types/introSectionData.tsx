export type IntroSectionData = {
  backgroundImageUrl: string;
  title: string;
  subText: string;
  dropdownMenuTitle: string;
  dropdownMenuItems: { name: string; pageSlug: string }[];
  rightButtonTitle: string;
};
